# BEGIN SYSTEM HTTPS SETTING
RewriteEngine on
RewriteOptions inherit
RewriteCond %{HTTPS} off
RewriteRule ^(.*)$ https://%{HTTP_HOST}%{REQUEST_URI} [R,L]
# END SYSTEM HTTPS SETTING

#RewriteEngine On
#RewriteRule (.*) https://www.kusaremkn.tk/$1 [R=302,L]
