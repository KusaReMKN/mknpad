/* 三角関数 */
const acos = Math.acos;
const acosf = Math.acos;
const acosl = Math.acos;

const asin = Math.asin;
const asinf = Math.asin;
const asinl = Math.asin;

const atan = Math.atan;
const atanf = Math.atan;
const atanl = Math.atan;

const atan2 = Math.atan2;
const atan2f = Math.atan2;
const atan2l = Math.atan2;

const cos = Math.cos;
const cosf = Math.cos;
const cosl = Math.cos;

const sin = Math.sin;
const sinf = Math.sin;
const sinl = Math.sin;

const tan = Math.tan;
const tanf = Math.tan;
const tanl = Math.tan;

/* 双曲線関数 */
const acosh = Math.acosh;
const acoshf = Math.acosh;
const acoshl = Math.acosh;

const asinh = Math.asinh;
const asinhf = Math.asinh;
const asinhl = Math.asinh;

const atanh = Math.atanh;
const atanhf = Math.atanh;
const atanhl = Math.atanh;

const cosh = Math.cosh;
const coshf = Math.cosh;
const coshl = Math.cosh;

const tanh = Math.tanh;
const tanhf = Math.tanh;
const tanhl = Math.tanh;

/* 指数・対数関数 */
const exp = Math.exp;
const expf = Math.exp;
const expl = Math.exp;

const exp2 = mknpad.sys.math.exp2;
const exp2f = mknpad.sys.math.exp2;
const exp2l = mknpad.sys.math.exp2;

const expm1 = Math.expm1;
const expm1f = Math.expm1;
const expm1l = Math.expm1;

const frexp = mknpad.sys.math.frexp;
const frexpf = mknpad.sys.math.frexp;
const frexpl = mknpad.sys.math.frexp;

const ilogb = mknpad.sys.math.ilogb;
const ilogbf = mknpad.sys.math.ilogb;
const ilogbl = mknpad.sys.math.ilogb;

const ldexp = mknpad.sys.math.ldexp;
const ldexpf = mknpad.sys.math.ldexp;
const ldexpl = mknpad.sys.math.ldexp;

const log = Math.log;
const logf = Math.log;
const logl = Math.log;

const log10 = Math.log10;
const log10f = Math.log10;
const log10l = Math.log10;

const log1p = Math.log1p;
const log1pf = Math.log1p;
const log1pl = Math.log1p;

const log2 = Math.log2;
const log2f = Math.log2;
const log2l = Math.log2;

const logb = mknpad.sys.math.logb;
const logbf = mknpad.sys.math.logb;
const logbl = mknpad.sys.math.logb;

const modf = mknpad.sys.math.modf;
const modff = mknpad.sys.math.modf;
const modfl = mknpad.sys.math.modf;

const FLT_RADIX = mknpad.sys.math.FLT_RADIX;

const scalbn = mknpad.sys.math.scalbn;
const scalbnf = mknpad.sys.math.scalbn;
const scalbnl = mknpad.sys.math.scalbn;

const scalbln = mknpad.sys.math.scalbln;
const scalblnf = mknpad.sys.math.scalbln;
const scalblnl = mknpad.sys.math.scalbln;

/* べき乗・絶対値関数 */
const cbrt = Math.cbrt;
const cbrtf = Math.cbrt;
const cbrtl = Math.cbrt;

const fabs = Math.abs;
const fabsf = Math.abs;
const fabsl = Math.abs;

const hypot = Math.hypot;
const hypotf = Math.hypot;
const hypotl = Math.hypot;

const pow = Math.pow;
const powf = Math.pow;
const powl = Math.pow;

const sqrt = Math.sqrt;
const sqrtf = Math.sqrt;
const sqrtl = Math.sqrt;

/* 誤差・ガンマ関数 */
const erf = mknpad.sys.math.erf;
const erff = mknpad.sys.math.erf;
const erfl = mknpad.sys.math.erf;

const erfc = mknpad.sys.math.erfc;
const erfcf = mknpad.sys.math.erfc;
const erfcl = mknpad.sys.math.erfc;

const lgamma = mknpad.sys.math.lgamma;
const lgammaf = mknpad.sys.math.lgamma;
const lgammal = mknpad.sys.math.lgamma;

const tgamma = mknpad.sys.math.tgamma;
const tgammaf = mknpad.sys.math.tgamma;
const tgammal = mknpad.sys.math.tgamma;

/* 最近接整数関数 */
const ceil = Math.ceil;
const ceilf = Math.ceil;
const ceill = Math.ceil;

const floor = Math.floor;
const floorf = Math.floor;
const floorl = Math.floor;

const nearbyint = Math.trunc;
const nearbyintf = Math.trunc;
const nearbyintl = Math.trunc;

const rint = Math.trunc;
const rintf = Math.trunc;
const rintl = Math.trunc;
const lrint = Math.trunc;
const lrintf = Math.trunc;
const lrintl = Math.trunc;
const llrint = Math.trunc;
const llrintf = Math.trunc;
const llrintl = Math.trunc;

const round = Math.round;
const roundf = Math.round;
const roundl = Math.round;
const lround = Math.round;
const lroundf = Math.round;
const lroundl = Math.round;
const llround = Math.round;
const llroundf = Math.round;
const llroundl = Math.round;

const trunc = Math.trunc;
const truncf = Math.trunc;
const truncl = Math.trunc;

/* 余剰関数 */
const fmod = mknpad.sys.math.fmod;
const fmodf = mknpad.sys.math.fmod;
const fmodl = mknpad.sys.math.fmod;

const remainder = mknpad.sys.math.remainder;
const remainderf = mknpad.sys.math.remainder;
const remainderl = mknpad.sys.math.remainder;

const remquo = mknpad.sys.math.remquo;
const remquof = mknpad.sys.math.remquo;
const remquol = mknpad.sys.math.remquo;

/* 実数捜査関数 */
const copysign = mknpad.sys.math.copysign;
const copysignf = mknpad.sys.math.copysign;
const copysignl = mknpad.sys.math.copysign;

const nan = mknpad.sys.math.nan;
const nanf = mknpad.sys.math.nan;
const nanl = mknpad.sys.math.nan;

const nextafter = mknpad.sys.math.nextafter;
const nextafterf = mknpad.sys.math.nextafter;
const nextafterl = mknpad.sys.math.nextafter;

const nexttoward = mknpad.sys.math.nexttoward;
const nexttowardf = mknpad.sys.math.nexttoward;
const nexttowardl = mknpad.sys.math.nexttoward;

/* 最大、最小及び正の差関数 */
const fdim = mknpad.sys.math.fdim;
const fdimf = mknpad.sys.math.fdim;
const fdiml = mknpad.sys.math.fdim;

const fmax = Math.max;
const fmaxf = Math.max;
const fmaxl = Math.max;

const fmin = Math.min;
const fminf = Math.min;
const fminl = Math.min;

/* 浮動小数点乗算加算 */
const fma = mknpad.sys.math.fma;
const fmaf = mknpad.sys.math.fma;
const fmal = mknpad.sys.math.fma;

/* マクロ */
const fpclassify = mknpad.sys.math.fpclassify;
const isfinite = Number.isFinite;
const isnan = Number.isNaN;
const isinf = mknpad.sys.math.isinf;
const isnormal = mknpad.sys.math.isnormal;
const signbit = mknpad.sys.math.signbit;

const isgreater = mknpad.sys.math.ingreater;
const isgreaterequal = mknpad.sys.math.isgreaterequal;
const isless = mknpad.sys.math.isless;
const islessequal = mknpad.sys.math.islessequal;
const islessgreater = mknpad.sys.math.islessgreater;
const isunordered = mknpad.sys.math.isunordered;
