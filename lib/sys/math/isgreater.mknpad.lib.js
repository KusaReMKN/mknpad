mknpad.sys = mknpad.sys || {};
mknpad.sys.math = mknpad.sys.math || {};
mknpad.sys.math.isgreater = mknpad.sys.math.isgreater || function (x, y) {
	return x > y;
}
mknpad.sys.math.isgreaterequal = mknpad.sys.math.isgreaterequal || function (x, y) {
	return x >= y;
}
mknpad.sys.math.isless = mknpad.sys.math.isless || function (x, y) {
	return x < y;
}
mknpad.sys.math.islessequal = mknpad.sys.math.islessequal || function (x, y) {
	return x <= y;
}
mknpad.sys.math.islessgreater = mknpad.sys.math.islessgreater || function (x, y) {
	return x != y;
}
